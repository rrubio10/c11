import Link from "next/link";
import { redirect } from "next/navigation";
import { getCurrentUser } from "@/lib/auth/session";
import { LogoutButton } from "@/components/ui/logout-button";
export default async function AppLayout({children}:{children:React.ReactNode}){const user=await getCurrentUser();if(!user)redirect('/login');return <div className="min-h-screen"><header className="sticky top-0 z-40 border-b border-slate-200 bg-white"><div className="mx-auto flex h-16 max-w-[1500px] items-center px-4 lg:px-7"><Link href="/dashboard" className="flex items-center gap-3"><span className="grid h-9 w-9 place-items-center rounded bg-[#007f86] font-black text-white">C1</span><span><b className="block leading-4">C1 Practice Lab</b><small className="text-slate-500">Reading & Use of English</small></span></Link><nav className="ml-8 hidden items-center gap-1 md:flex"><Link className="rounded px-3 py-2 text-sm hover:bg-slate-100" href="/dashboard">Dashboard</Link><Link className="rounded px-3 py-2 text-sm hover:bg-slate-100" href="/library">Library</Link><Link className="rounded px-3 py-2 text-sm hover:bg-slate-100" href="/progress">Progress</Link>{user.role==='ADMIN'&&<Link className="rounded px-3 py-2 text-sm hover:bg-slate-100" href="/admin">Admin</Link>}</nav><div className="ml-auto flex items-center gap-3"><span className="hidden text-sm text-slate-600 sm:inline">{user.name}</span><LogoutButton/></div></div></header>{children}<footer className="border-t bg-white px-4 py-5 text-center text-xs text-slate-500">Independent educational platform. Not affiliated with or officially endorsed by Cambridge University Press & Assessment.</footer></div>}
