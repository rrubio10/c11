"use client";
import { useState } from "react";
export function AdminImport(){const [result,setResult]=useState<string>('');const [busy,setBusy]=useState(false);async function upload(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const input=e.currentTarget.elements.namedItem('file') as HTMLInputElement;const file=input.files?.[0];if(!file)return;setBusy(true);const content=await file.text();const r=await fetch('/api/admin/import',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({sourceName:file.name,content})});const d=await r.json();setResult(JSON.stringify(d,null,2));setBusy(false)}return <form onSubmit={upload} className="space-y-4"><label className="block text-sm font-bold">Master TXT file<input name="file" type="file" accept=".txt,text/plain" required className="mt-2 block w-full rounded-md border border-slate-300 bg-white p-3"/></label><button disabled={busy} className="rounded-md bg-[#007f86] px-4 py-2.5 font-bold text-white">{busy?'Importing…':'Validate and import'}</button>{result&&<pre className="max-h-80 overflow-auto rounded-lg bg-slate-950 p-4 text-xs text-slate-100">{result}</pre>}</form>}
